CREATE TABLE transactions (
    origin varchar(4),
    destination varchar(4),
    transaction_date date,
    price float,
    unit varchar,
    quantity int,
    product_category varchar,
    description varchar
)